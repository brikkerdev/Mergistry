

# Итерации разработки — Alchemist's Descent

Ниже — 4 файла для каждой фазы. Каждая итерация содержит 2–3 фичи, тестируемые в Play Mode.

---

## Файл 1: `iterations_mvp.md`

```markdown
# MVP — Итерации

> **Цель фазы**: играбельный прототип — один этаж, 3 боя линейно,
> merge работает, зелья наносят урон, можно выиграть или умереть.
>
> **Длительность**: 4–5 недель (6 итераций × 3–5 дней)

---

## M1 — Фундамент и доска

**Цель**: на экране доска 4×4, ингредиенты можно перетаскивать.

### Фичи

1. **Инфраструктура загрузки**
   - Boot-сцена, Game-сцена (аддитивная загрузка)
   - ServiceLocator с регистрацией пустых заглушек сервисов
   - EventBus (publish / subscribe / unsubscribe)
   - GameStateMachine с двумя состояниями: `MenuState` (кнопка «Старт»)
     и `DistillationState`

2. **Доска перегонки — модель и вид**
   - `BoardModel`: сетка 4×4, `CellContent` (type, elementType)
   - `DistillationService.GenerateBoard`: заполнение 3 элементами
     (Ignis, Aqua, Toxin), равномерное распределение, seed
   - `BoardView`: 16 quad-ов, цветные круги (placeholder — unlit
     solid color material)
   - `IngredientView`: позиция привязана к клетке, idle-покачивание
     (sin по Y)

3. **Drag & Drop ингредиентов**
   - `InputService`: распознавание DragStart / DragUpdate / DragEnd
   - `BoardView`: определение ингредиента под пальцем
     (worldPos → gridPos), перемещение визуала за пальцем
   - Подсветка валидных соседних клеток (зелёная рамка)
   - Bounce-back при отпускании вне валидной цели
   - Счётчик действий (3 точки, не уменьшается — merge ещё нет)

### Приёмочный тест (Play Mode)

```
1. Запустить → экран меню с кнопкой «Старт»
2. Нажать «Старт» → доска 4×4 с цветными кругами (3 цвета)
3. Зажать палец на ингредиенте → увеличивается, соседние
   клетки подсвечиваются
4. Перетащить на соседнюю клетку → bounce-back
   (merge ещё не работает)
5. Отпустить вне цели → вернулся на место
6. Три точки действий видны, не уменьшаются
```

---

## M2 — Merge, подпитка, инвентарь

**Цель**: можно мержить ингредиенты, получать варева,
подпитывать их, собирать в инвентарь.

### Фичи

1. **Merge-логика**
   - `DistillationService.CanMerge` и `PerformMerge`
   - Рецепты: 3 базовых (🔴+🔴, 🔵+🔵, 🟢+🟢)
     и 3 рецептурных (Пар, Напалм, Кислота)
   - При merge: оба ингредиента потребляются, варево lv1 появляется
     в клетке назначения, пустая клетка в источнике
   - `BrewView`: визуально отличается от ингредиента
     (прямоугольник вместо круга, другой цвет бордюра)
   - Подпитка: `CanInfuse` и `PerformInfuse` — ингредиент → соседнее
     варево, +1 уровень, валидация элемента
   - Счётчик действий уменьшается при merge и подпитке

2. **Инвентарь зелий**
   - `InventoryModel`: 4 слота, PotionSlot (type, level, cooldown)
   - `PotionSlotView`: 4 прямоугольника внизу экрана, цвет зелья,
     точки уровня
   - `DistillationService.CollectBrews`: при завершении фазы —
     все варева → инвентарь
   - Кнопка «В бой →» (завершает фазу досрочно)

3. **Обработка конфликтов инвентаря**
   - Если зелье того же типа уже есть — обновить уровень (если выше)
   - Если нет свободного слота — popup выбора замены
     (SlotReplacePopup: показать все слоты + новое зелье,
     тап = заменить)

### Приёмочный тест (Play Mode)

```
1. Доска 4×4, три элемента
2. Перетащить 🔴 на соседний 🔴 →
    - Оба исчезают
    - В клетке назначения — варево «Пламя lv1» (другой вид)
    - Клетка источника пустая
    - Точка действия погасла (осталось 2)
3. Перетащить 🔴 на варево «Пламя» →
    - Ингредиент исчезает
    - Варево стало «Пламя lv2» (2 точки)
    - Осталось 1 действие
4. Выполнить ещё один merge (🔵+🟢 = Кислота lv1)
    - Осталось 0 действий
5. Кнопка «В бой →» нажать →
   варева летят в слоты инвентаря →
   слоты показывают: Пламя lv2, Кислота lv1, пусто, пусто
6. Повторить с полным инвентарём →
   popup выбора замены появляется
```

---

## M3 — Боевая сетка и движение

**Цель**: после перегонки появляется сетка 5×5 с персонажем,
который двигается свайпами.

### Фичи

1. **Боевая сетка**
   - `CombatModel`: `GridModel` 5×5, `PlayerCombatModel`
   - `CombatService.InitCombat`: создание модели, расстановка игрока
     в (1,1)
   - `GridView`: 25 квадратов (placeholder — тёмно-серые quad-ы
     с тонкими линиями-разделителями)
   - `PlayerView`: яркий круг (placeholder) в клетке

2. **Движение игрока**
   - `InputService`: распознавание Swipe (от персонажа)
   - `CombatService.GetValidMoves`: все клетки в радиусе 2
     (горизонталь + вертикаль + L-форма), не через стены
   - Призрак (полупрозрачный дубликат) показывает конечную позицию
     до отпускания
   - `PlayerView`: tween-перемещение к целевой клетке (0.2s, lerp)
   - `CombatModel.player.hasMoved = true` после движения

3. **Переход Distillation → Combat**
   - `GameStateMachine`: переход `DistillationState → CombatState`
   - Простой fade (0.2s чёрный экран)
   - Доска скрывается, сетка появляется
   - HUD: слоты зелий остаются видны

### Приёмочный тест (Play Mode)

```
1. Меню → перегонка → сделать merge → «В бой →»
2. Экран темнеет → появляется сетка 5×5 с персонажем в (1,1)
3. Свайп вправо от персонажа → призрак в (2,1) или (3,1)
4. Отпустить → персонаж плавно перемещается
5. Свайп вверх → перемещение вертикально
6. Слоты зелий видны внизу, с зельями из перегонки
7. Повторный свайп → персонаж не двигается
   (уже ходил в этом ходу) [пока нет врагов — ход не кончается,
   это ОК, будет доработано в M5]
```

---

## M4 — Бросок зелий и кулдауны

**Цель**: можно выбрать зелье, бросить в клетку,
увидеть эффект, зелье уходит в кулдаун.

### Фичи

1. **Выбор и бросок зелья**
   - Тап на `PotionSlotView` → слот выделяется (яркий контур),
     на сетке подсвечиваются допустимые клетки (синий оверлей)
   - `DamageService.GetAffectedCells`: вычисление AoE
     (крест для Пламени, линия для Потока, 2×2 для Отравы)
   - Тап на допустимую клетку → `CombatService.ThrowPotion`
   - Отображение AoE-зоны (красный оверлей на affected cells)
     при наведении перед подтверждением
   - `EventBus.Publish(PotionThrown)` → `EffectView` (цветная
     вспышка в клетке, placeholder — расширяющийся круг
     цвета зелья, fade out за 0.3s)

2. **Кулдаун**
   - После броска: `slot.cooldownRemaining = 2` (или 1 для lv3)
   - `PotionSlotView`: затемнение + число ходов
   - Тап на зелье в кулдауне — ничего не происходит
     (слот не подсвечивается)
   - Тик кулдауна: пока вручную (кнопка «следующий ход»
     уменьшает кулдауны)

3. **Кнопка пропуска хода**
   - Кнопка ⏭️ под сеткой
   - Нажатие: `CombatService.SkipTurn` → +1 HP (если < max),
     все кулдауны −1
   - Сброс `hasMoved` и `hasActed` (новый ход)

### Приёмочный тест (Play Mode)

```
1. Перегонка: создать Пламя lv1 и Поток lv1
2. В бою: тап на слот Пламя → слот выделяется,
   клетки в радиусе 3 подсвечиваются синим
3. Тап на клетку → вспышка в крестообразной зоне (5 клеток),
   Пламя тускнеет, число «2» на слоте
4. Тап на Пламя → ничего (кулдаун)
5. Тап на Поток → подсвечивается, тап на клетку → линейный эффект
6. Нажать ⏭️ → кулдауны: Пламя = 1, Поток = 1
7. Нажать ⏭️ → кулдауны: Пламя = 0 (готово), Поток = 0 (готово)
8. Пламя снова доступно для броска
```

---

## M5 — Враги и пошаговый бой

**Цель**: на сетке есть скелет и паук, они ходят по правилам,
можно убить зельем, можно получить урон.

### Фичи

1. **Скелет**
   - `EnemyCombatModel`: entityId, position, HP=3, definition
   - `SkeletonBehavior`: определение intent-а
     (если рядом → ⚔️ atk 2, иначе → 🚶 move к игроку)
   - `EnemyView`: placeholder (белый квадрат с «глазами» — 
     две маленькие точки)
   - `IntentionIconView`: иконка над врагом (⚔️ или →) +
     красная подсветка целевых клеток

2. **Паук**
   - `SpiderBehavior`: определение линии к игроку,
     intent = ⚔️ по линии (дальность 3), урон 1
   - `EnemyView`: placeholder (серый овал)
   - Красная подсветка линии атаки

3. **Пошаговый цикл и урон**
   - `CombatState` подсостояния:
     `WaitMove → WaitAction → AnimatePlayer → ResolveEnemy → Check`
   - `DamageService.ApplyDamage`: урон по врагу (HP -= damage,
     с учётом уровня зелья), смерть при HP ≤ 0
   - `DamageService.ApplyDamageToPlayer`: урон игроку
   - При попадании зельем: враг мигает белым (0.1s)
   - При смерти врага: враг исчезает (fade out 0.3s)
   - `AIService.DetermineIntents`: вызывается в конце каждого хода,
     обновляет intent-ы
   - `AIService.ExecuteIntents`: выполняет intent-ы всех врагов
   - Порядок хода: движение игрока → действие игрока →
     ходы всех врагов → тик кулдаунов → новый ход

### Приёмочный тест (Play Mode)

```
1. Перегонка: создать Пламя lv1
2. Бой: на сетке 1 скелет (3,3) и 1 паук (4,1)
3. Скелет показывает → (идёт к игроку),
   паук показывает ⚔️ + красная горизонтальная линия
4. Сдвинуться с линии паука (свайп вверх)
5. Бросить Пламя в скелета → скелет мигает, HP -= 3, скелет умирает
6. Нажать ⏭️ → ход врагов: паук стреляет (промахивается,
   игрока нет на линии)
7. Дождаться кулдауна Пламени → бросить в паука → паук умирает
8. Если встать на линию паука и пропустить ход →
   паук стреляет, игрок получает 1 урон
```

---

## M6 — Игровой цикл и смерть

**Цель**: полный цикл: меню → перегонка → бой → перегонка → бой →
перегонка → бой → результат.

### Фичи

1. **HP-дисплей и смерть**
   - `HealthBarView`: 5 сердечек (placeholder — красные/серые круги)
   - Обновление при `PlayerDamaged`: сердечко гаснет, красная
     вспышка по краям экрана (0.2s)
   - HP ≤ 0 → `CombatEnded(Defeat)` → переход в `ResultState`
   - `ResultScreen`: «Вы пали на Этаже 1, бой 2», кнопки
     «Ещё раз» и «Меню»

2. **Линейная структура рана (3 боя)**
   - `RunModel`: currentFight (0, 1, 2), HP сохраняется между боями
   - Последовательность: Distill → Fight1 (1 скелет) →
     Distill → Fight2 (2 скелета) →
     Distill → Fight3 (1 скелет + 1 паук) → Victory
   - `CombatEnded(Victory)` → если currentFight < 2:
     следующая перегонка → следующий бой
   - Если currentFight == 2 → `ResultScreen` с победой
   - Между боями HP **не восстанавливается**

3. **Базовые SDF-шейдеры**
   - `SH_Background`: вертикальный gradient (тёмно-синий)
   - `SH_Floor`: solid color + тонкие линии сетки
   - `SH_Player`: SDF круг + треугольник (плащ), solid color
   - `SH_Skeleton`: SDF прямоугольник + круг, точки-глаза
   - `SH_Spider`: SDF овал + 8 линий-ног
   - `SH_Ingredient`: SDF круг, цвет по элементу
   - `SH_Brew`: SDF прямоугольник + круг (колба), цвет зелья
   - `AlchemistSDF.cginc`: базовые SDF-примитивы
     (sdCircle, sdBox, sdTriangle, sdSegment, opUnion)

### Приёмочный тест (Play Mode)

```
1. Меню → «Старт» → перегонка (доска 4×4)
2. Сделать merge → зелья в инвентаре → «В бой»
3. Бой 1: 1 скелет → убить → победа
4. Автоматически → перегонка → «В бой»
5. Бой 2: 2 скелета → убить обоих → победа
6. Перегонка → «В бой»
7. Бой 3: 1 скелет + 1 паук → убить → победа →
   «Вы победили!», кнопка «Ещё раз»
8. Нажать «Ещё раз» → новый ран с начала
9. Альтернативно: получить урон, умереть →
   «Вы пали», кнопки «Ещё раз» / «Меню»
10. Все сущности рисуются SDF-шейдерами
    (геометрические фигуры, не квадраты)
```

---

## Чеклист завершения MVP

- [ ] Полный цикл: меню → перегонка → бой ×3 → результат
- [ ] Merge: одинаковые → базовое зелье, разные → рецептурное
- [ ] Подпитка: ингредиент → варево, +1 уровень, валидация элемента
- [ ] 3 действия за фазу перегонки, счётчик работает
- [ ] 3 базовых + 3 рецептурных зелья (Пламя, Поток, Отрава,
      Пар, Напалм, Кислота)
- [ ] Бросок зелья с AoE (крест, линия, 2×2)
- [ ] Кулдаун 2 хода (1 для lv3)
- [ ] Движение 0–2 клетки
- [ ] Пропуск хода (+1 HP)
- [ ] Скелет: идёт к игроку, атакует вблизи
- [ ] Паук: стоит на месте, стреляет по линии
- [ ] Намерения врагов отображаются
- [ ] HP 5, смерть, экран результата
- [ ] HP не восстанавливается между боями
- [ ] SDF-шейдеры для всех сущностей (силуэты)
- [ ] 60 FPS на целевом устройстве
```

---

## Файл 2: `iterations_alpha.md`

```markdown
# Alpha — Итерации

> **Цель фазы**: полный ран от начала до финального босса.
> Все типы контента, карта с развилками, события, реликвии, 3 этажа.
>
> **Предусловие**: MVP завершён и стабилен.
>
> **Длительность**: 5–6 недель (8 итераций × 3–5 дней)

---

## A1 — Все элементы и рецепты

**Цель**: на доске 5 элементов, все 15 зелий создаются корректно.

### Фичи

1. **Элементы Lux и Umbra**
   - Добавить в `ElementType`, цвета, placeholder-шейдеры
     (жёлтый круг, фиолетовый круг)
   - `DistillationService.GenerateBoard`: пул элементов зависит
     от этажа (3 / 4 / 5)
   - Базовые зелья: Сияние (🟡+🟡), Мрак (🟣+🟣) — AoE,
     урон, эффекты

2. **Все рецептурные зелья**
   - Все 10 рецептов в `RecipeDB` (ScriptableObject)
   - `PotionDefinition` SO для каждого: базовый урон, AoE-паттерн,
     эффект, описание
   - `DamageService.GetAffectedCells`: все AoE-паттерны
     (крест, линия, облако, область, одиночная)
   - Разные эффекты зелий: урон, DoT, оглушение, ослепление,
     замедление, телепорт, проклятие, хаос

3. **Книга рецептов (базовая)**
   - `BookScreen`: список всех рецептов, разделённый на базовые
     и рецептурные
   - Карточка зелья: название, рецепт, AoE, урон по уровням,
     описание эффекта
   - Кнопка 📖 в HUD → открытие книги
   - Неразблокированные рецепты: «?» (подготовка к мета-прогрессии,
     но пока все открыты)

### Приёмочный тест (Play Mode)

```
1. Начать ран → доска с 3 элементами (этаж 1)
2. Создать Пламя, Поток, Отраву — базовые работают
3. Создать Пар (🔴+🔵), Напалм (🔴+🟢), Кислоту (🔵+🟢) —
   рецептурные работают
4. Открыть книгу (📖) → все 15 зелий видны с описаниями
5. Принудительно перейти на этаж 2 (debug) →
   на доске 4 элемента (+🟡)
6. Создать Молнию (🔵+🟡) → линейный AoE работает
7. На этаже 3 → 5 элементов, создать Хаос (🟡+🟣) →
   случайный эффект
8. Каждое зелье наносит правильный урон (проверить
   по таблице GDD)
```

---

## A2 — Враги (часть 1)

**Цель**: Гриб-бомба, Магнит-голем и Бронированный жук
работают по правилам.

### Фичи

1. **Гриб-бомба**
   - `MushroomBombBehavior`: таймер 3→2→1→💥, взрыв 3×3, урон 5
   - `EnemyView`: placeholder SDF (полукруг + прямоугольник),
     цвет меняется (жёлтый → красный по таймеру)
   - Число-таймер над головой
   - Взрыв наносит урон и врагам (можно использовать тактически)

2. **Магнит-голем и Бронированный жук**
   - `MagnetGolemBehavior`: притяжение игрока на 1 клетку,
     контактный урон 2, HP 6
   - `ArmoredBeetleBehavior`: как скелет + система брони
   - `DamageService.ApplyDamage`: обработка брони
     (armorPoints поглощают урон первыми)
   - Визуал брони: индикатор (ромбики над врагом),
     гаснут при снятии
   - Эффект Кислоты: снятие брони (armorPoints = 0)

3. **Толчок врага**
   - `CombatService.PushEnemy`: свайп от врага на соседней клетке
   - Враг сдвигается на 1 клетку в направлении толчка
   - Если за врагом стена → +1 урон
   - Анимация толчка: tween позиции + squash
   - InputService: распознавание свайпа от врага (vs свайп от игрока)

### Приёмочный тест (Play Mode)

```
1. Бой с 1 грибом-бомбой + 2 скелетами:
    - Гриб показывает «3» → «2» → «1» → взрыв 3×3
    - Скелеты в зоне взрыва получают 5 урона и умирают
    - Игрок в зоне взрыва получает 5 урона
2. Бой с магнит-големом:
    - Каждый ход игрока притягивает на 1 клетку к голему
    - При контакте — 2 урона
    - Убить зельем lv2+ (HP = 6)
3. Бой с бронированным жуком:
    - Бросить Пламя lv1 (3 урона) → жук потерял 2 брони + 1 HP
    - Бросить Кислоту → броня снята, теперь полный урон
4. Толкнуть скелета → сдвинулся на 1 клетку
5. Толкнуть скелета в стену → сдвинулся + 1 доп. урон
```

---

## A3 — Враги (часть 2) и зоны

**Цель**: Зеркальный слайм, Фантом, Некромант работают.
Зоны на поле (огонь, вода, яд) функционируют.

### Фичи

1. **Зеркальный слайм, Фантом, Некромант**
   - `MirrorSlimeBehavior`: копирует последнее зелье игрока
     (из CombatModel.combatLog), бросает обратно
   - `PhantomBehavior`: телепортация на случайную клетку
     в радиусе 2 от игрока, атака
   - `NecromancerBehavior`: воскрешение убитого врага (1 HP),
     движение от игрока
   - Placeholder SDF для каждого

2. **Зоны на поле**
   - `ZoneInstance`: тип, позиция, turnsRemaining
   - `GridModel.cells[].zones`: список зон на клетке
   - Зелья создают зоны: Пламя → огонь (2 хода),
     Поток → вода (3 хода), Отрава → яд (3 хода)
   - `CombatService.EndTurn`: тик зон (decrement duration),
     применение zone damage (1 урон огонь/яд за ход)
   - `ZoneOverlayView`: полупрозрачный цветной квадрат
     на тайле (placeholder)
   - Зоны влияют на **всех** (игрока тоже)

3. **Статус-эффекты**
   - `StatusEffect`: тип + duration
   - Замедление (движение −1): от паутины, воды
   - Оглушение (пропуск хода): от Вспышки
   - Отравление (DoT): от Отравы, Напалма
   - `CombatService.EndTurn`: тик эффектов,
     применение DoT к отравленным

### Приёмочный тест (Play Mode)

```
1. Бой с зеркальным слаймом:
    - Бросить Пламя → слайм показывает «скопирую Пламя»
    - Ход врагов → слайм бросает Пламя по игроку
    - Бросить Пар (0 урона) → слайм копирует Пар (безобидно)
2. Бой с фантомом:
    - Фантом показывает зону возможных позиций (кольцо вокруг игрока)
    - Ход врагов → телепортация + атака
3. Бой с некромантом + 2 скелетами:
    - Убить скелета → некромант воскрешает его с 1 HP
    - Убить некроманта → воскрешение прекращается
4. Бросить Пламя → зона огня (оранжевый оверлей) на 5 клетках
5. Враг входит в зону огня → получает 1 урон
6. Игрок входит в зону огня → тоже получает 1 урон
7. Через 2 хода зона огня исчезает
8. Бросить Отраву → зона яда + враг отравлен (DoT 1/ход)
```

---

## A4 — Карта и многоэтажность

**Цель**: между боями — карта этажа с развилками.
Три этажа, переход между ними.

### Фичи

1. **Генерация карты**
   - `MapGeneratorService.GenerateFloor`: алгоритм из архитектурного
     документа (3–4 ряда, развилки, 1 элита, 1–2 события, 1 босс)
   - `FloorMapModel`: узлы, связи, типы
   - `MapNodeView`: круги с иконками (⚔️ ⭐ ❓ 👑)
   - `MapPathView`: линии между узлами

2. **Навигация по карте**
   - `MapState`: отображение карты, обработка тапа на узел
   - Доступные узлы подсвечены, недоступные — тусклые
   - Тап на доступный узел → переход:
     ⚔️ → DistillationState → CombatState,
     ❓ → EventState (заглушка),
     ⭐ → DistillationState → CombatState (усиленные враги),
     👑 → DistillationState → BossState (заглушка)
   - После победы → возврат на карту, узел помечен как пройденный

3. **Три этажа**
   - `RunModel.currentFloor`: 0, 1, 2
   - Победа над боссом → следующий этаж (новая карта)
   - Этаж 3, босс побеждён → Victory (ResultScreen)
   - Составы врагов зависят от этажа
     (`MapGeneratorService.GenerateCombatSetup`)
   - Пул элементов на доске зависит от этажа (3 / 4 / 5)

### Приёмочный тест (Play Mode)

```
1. Меню → «Старт» → карта этажа 1:
   узлы с иконками, линии-пути, «ТЫ ЗДЕСЬ» внизу
2. Тап на ⚔️ узел → перегонка → бой → победа → карта
3. Пройденный узел тусклый с галочкой
4. Развилка: два узла доступны, выбрать ⭐ (элиту) →
   усиленный бой (3 врага)
5. Дойти до 👑 (босс) → бой (заглушка — обычные враги,
   сетка 5×5)
6. Победа → «Этаж 2» → новая карта, 4 элемента на доске
7. Пройти этаж 2 → этаж 3 → 5 элементов
8. Победить «босса» этажа 3 → Victory screen
```

---

## A5 — События и реликвии

**Цель**: события (фонтан, торговец, алтарь, сундук) работают.
После элиты — выбор реликвии.

### Фичи

1. **Четыре типа событий**
   - `EventState`: показ экрана события, обработка выбора
   - `EventScreen`: фон + описание + кнопки выбора
   - ⛲ Фонтан: [Выпить] 70% полное лечение / 30% −1 maxHP,
     [Наполнить] случайное зелье lv2
   - 🛒 Торговец: 3 зелья, цена 1–2 HP, купить одно
   - ⚗️ Алтарь: удалить зелье → случайная реликвия
   - 🎁 Сундук: выбор 1 из 3 (зелье / +3 HP / +1 действие)
   - `LootService.GenerateEventPotion`,
     `LootService.GenerateMerchantStock`

2. **Реликвии (5 штук)**
   - `RelicDefinition` SO: id, название, описание, эффект
   - `RelicManager`: список активных реликвий, проверка наличия
   - Реализовать 5 из 10: Термос, Линза, Фляга, Кубик, Призма
   - `RelicChoicePopup`: показ 3 реликвий после элиты,
     тап = выбор
   - Иконки реликвий в HUD (маленькие, рядом с HP)
   - Эффекты: Термос (25% не тратить кулдаун),
     Линза (AoE +1), Фляга (+1 HP после боя),
     Кубик (+1 действие перегонки), Призма (combo ×1.5)

3. **Интеграция реликвий в сервисы**
   - `DamageService`: проверка Линзы (расширить AoE),
     Призмы (×1.5 combo)
   - `CombatService.ThrowPotion`: проверка Термоса
     (шанс не тратить кулдаун)
   - `CombatService.EndCombat`: проверка Фляги (+1 HP)
   - `DistillationService`: проверка Кубика (+1 действие)

### Приёмочный тест (Play Mode)

```
1. На карте узел ❓ → тап → EventScreen «Фонтан»
2. [Выпить] → лечение (или потеря maxHP, 30%)
3. Торговец: 3 зелья с ценой, купить одно → HP уменьшается,
   зелье в инвентаре
4. Алтарь: удалить зелье → реликвия появляется в HUD
5. Сундук: выбрать +3 HP → HP увеличивается
6. Победить элиту → RelicChoicePopup:
   3 реликвии, тап → реликвия в HUD
7. С реликвией Фляга: после боя +1 HP
8. С реликвией Кубик: на перегонке 4 действия вместо 3
9. С реликвией Линза: AoE Пламени — 9 клеток вместо 5
```

---

## A6 — Боссы

**Цель**: три уникальных босса с двумя фазами каждый.

### Фичи

1. **Босс-фреймворк**
   - `BossState`: расширение CombatState
   - Сетка 6×6 для босс-боёв
   - `IBossBehavior`: интерфейс с поддержкой фаз
     (`DetermineIntent` получает текущую фазу)
   - Переключение фазы при HP ≤ 50%: визуальная отбивка
     (экран мигает, текст «Фаза 2»)
   - Босс занимает 2×2 клетки (4 клетки, все считаются occupant-ом)
   - Boss HP bar вверху экрана (горизонтальная полоса)

2. **Три босса**
   - Королева пауков (этаж 1): паутина + линейные атаки,
     фаза 2: призыв пауков + отступление
   - Железный голем (этаж 2): удар 2×2 + притяжение + броня 6,
     фаза 2: ломает столбы + ускорение
   - Алхимик-Отступник (этаж 3): копирование зелий + зоны,
     фаза 2: котлы генерируют зоны + телепорт
   - Placeholder SDF для каждого (увеличенные версии обычных врагов)
   - Интерактивные объекты: столбы (непроходимые),
     котлы (HP 3, уничтожаемые)

3. **Столбы и котлы**
   - `InteractableModel`: позиция, HP (для котлов),
     isPassable (false для столбов)
   - Зелья могут попадать в котлы → наносят урон
   - Уничтожение столба (босс голем, фаза 2): AoE осколков 2×2
   - Уничтожение котла (Алхимик): лишает телепортации

### Приёмочный тест (Play Mode)

```
1. Дойти до босса этажа 1 → сетка 6×6,
   Королева пауков (2×2) + 3 клетки с паутиной
2. Фаза 1: каждый ход 2 линии паутины,
   движение к игроку
3. Снизить HP до 50% → «Фаза 2»,
   призываются 2 паука, королева отступает
4. Убить → переход на этаж 2
5. Босс этажа 2: Железный голем, 2 столба на поле
6. Фаза 1: удар 2×2, притяжение, столбы блокируют
7. Фаза 2: столбы ломаются (AoE), голем ускоряется
8. Босс этажа 3: Алхимик, 3 котла
9. Уничтожить котлы → телепортация прекращается
10. Убить финального босса → Victory screen
```

---

## A7 — Combo и модификаторы комнат

**Цель**: зелья взаимодействуют с зонами (combo).
Комнаты имеют модификаторы.

### Фичи

1. **Combo-система (3 из 7)**
   - `DamageService.CheckCombo`: проверка зелья + зоны
     на целевой клетке
   - Молния + Вода: ×2 урон всем на воде
   - Огонь + Яд: облако яда расширяется
   - Поток + Лёд: заморозка врагов на воде
   - Лёд создаётся при Поток + Лёд combo (пока единственный
     источник льда)
   - При combo: screenshake (5px, 0.2s) + текст «COMBO»
     (scale up → fade)

2. **Модификаторы комнат (3 из 6)**
   - `RoomModifier` в `CombatSetup`
   - 🌊 Затопленная: вся сетка = вода (молния ×2, огонь ×0.5)
   - 🔥 Горящая: 1–2 случайные клетки загораются каждый ход
   - 🕳 С ямами: 2–3 непроходимые клетки,
     толчок врага в яму = мгновенная смерть
   - Иконка модификатора на узле карты
   - `CombatService.EndTurn`: логика горящей комнаты
   - `CombatService.PushEnemy`: проверка ямы

3. **Зона льда**
   - `ZoneType.Ice`: скольжение (существо проезжает +1 клетку)
   - Применяется при движении через зону льда
   - Placeholder визуал (голубой оверлей)

### Приёмочный тест (Play Mode)

```
1. Бросить Поток → зона воды на 4 клетках
2. Бросить Молнию в клетку с водой → «COMBO!»,
   все враги на воде получают ×2 урон
3. Бросить Пламя в клетку с ядом → облако яда расширяется
4. Комната 🌊 (затопленная): вся сетка — вода,
   Молния ×2 ко всем
5. Комната 🔥 (горящая): в конце хода 1–2 клетки загораются
6. Комната 🕳 (с ямами): 2 чёрные клетки,
   толкнуть врага в яму → мгновенная смерть
7. Зона льда: игрок входит → проезжает на 1 клетку дальше
```

---

## A8 — Полные шейдеры и сохранение

**Цель**: все сущности имеют полные SDF-шейдеры с наполнением.
Bloom работает. Прогресс сохраняется.

### Фичи

1. **SDF-шейдеры сущностей (полные)**
   - `SH_Player`: flow noise внутри плаща, цвет последнего зелья
   - `SH_Skeleton`: мерцающие глаза (sin pulse), рёбра
   - `SH_Spider`: анимированные ноги (sin per leg)
   - `SH_MushroomBomb`: lerp цвета по таймеру, ускоряющаяся пульсация
   - `SH_MagnetGolem`: voronoi текстура, волны притяжения (ring SDF)
   - `SH_MirrorSlime`: metaballs + HSV hue shift
   - `SH_ArmoredBeetle`: шестиугольник + specular
   - `SH_Phantom`: мерцающая alpha + trail
   - `SH_Necromancer`: спиральный вихрь + руны
   - `SH_Ingredient`: заполнение по типу элемента
     (fBm для огня, волны для воды и т.д.)
   - `SH_Brew`: колба с жидкостью, пузырьки, уровень = высота
     заполнения
   - Зонные шейдеры: `SH_Zone_Fire`, `SH_Zone_Water`,
     `SH_Zone_Poison`, `SH_Zone_Ice`

2. **Bloom пост-процессинг**
   - `SH_PP_BloomExtract`, `SH_PP_BloomBlur`, `SH_PP_BloomCombine`
   - `PostProcessController` на камере: OnRenderImage pipeline
   - Bloom threshold = 0.8, intensity = 1.5
   - Все glow-элементы (зелья, варева, эффекты) ярче порога → bloom

3. **Система сохранения**
   - `SaveService`: SaveMeta, LoadMeta, SaveRun, LoadRun
   - `meta.json`: пока минимальный (stats, settings)
   - `run.json`: mid-run save (при OnApplicationPause,
     после каждого боя)
   - Загрузка при старте: если есть run.json → popup
     «Продолжить прерванный ран?»
   - Удаление run.json при завершении рана

### Приёмочный тест (Play Mode)

```
1. Все сущности визуально отличимы и анимированы:
    - Огненные ингредиенты мерцают
    - Водные ингредиенты волнятся
    - Варева — колбы с жидкостью, уровень виден
    - Глаза скелета мерцают
    - Ноги паука шевелятся
    - Слайм переливается
    - Фантом мерцает
    - Голем имеет волны притяжения
2. Bloom: все светящиеся объекты имеют ореол свечения
3. Зоны на поле: огонь = анимированное пламя (noise),
   вода = волны, яд = пузырьки
4. Закрыть приложение mid-run → открыть → popup →
   «Продолжить» → ран продолжается с того же места
5. Завершить ран → run.json удалён
```

---

## Чеклист завершения Alpha

- [ ] 5 элементов, 5 базовых зелий, 10 рецептурных зелий
- [ ] 8 типов врагов, все с уникальным поведением
- [ ] 3 босса с 2 фазами каждый
- [ ] 3 этажа, карта с развилками
- [ ] 4 типа событий
- [ ] 5 реликвий
- [ ] 3 combo-эффекта
- [ ] 3 модификатора комнат
- [ ] Зоны на поле (4 типа)
- [ ] Статус-эффекты (замедление, оглушение, отравление)
- [ ] Полные SDF-шейдеры для всех сущностей
- [ ] Bloom пост-процессинг
- [ ] Save/Load (мета + mid-run)
- [ ] Полный ран от меню до финальной победы проходим
```

---

## Файл 3: `iterations_beta.md`

```markdown
# Beta — Итерации

> **Цель фазы**: полная игра с мета-прогрессией, обучением,
> juice-эффектами, балансом. Готовность к внешним плейтестам.
>
> **Предусловие**: Alpha завершена — полный контент, стабильный цикл.
>
> **Длительность**: 4–5 недель (7 итераций × 3–4 дня)

---

## B1 — Полные combo и модификаторы

**Цель**: все 7 combo и все 6 модификаторов комнат работают.

### Фичи

1. **Оставшиеся 4 combo**
   - Огонь + Лёд → Пар (ослепление всех в зоне)
   - Молния + Огонь → Огненная буря (усиленный AoE)
   - Кислота + Вода → Кислотный потоп (все клетки воды наносят урон)
   - Яд + Огонь → Токсичный дым (3×3 облако DoT + ослепление)
   - Обновить `DamageService.CheckCombo` для всех пар

2. **Оставшиеся 3 модификатора**
   - ⏳ Таймер: через 6 ходов — 99 урона всем.
     Счётчик на экране (большие цифры), пульсация при ≤2
   - 🌑 Тёмная: видимость 2 клетки от игрока, остальное —
     чёрный fog of war (шейдерная маска)
   - 🔄 Конвейер: все сдвигаются на 1 клетку в указанном
     направлении. Стрелки на полу

3. **Индикация модификаторов**
   - Иконка на узле карты (видна до входа)
   - Текст-подсказка при входе в комнату
     («Эта комната затоплена!» — 1.5 сек, fade out)

### Приёмочный тест (Play Mode)

```
1. Бросить Пламя в зону льда → «COMBO! Пар» →
   ослепление всех в зоне
2. Бросить Молнию в зону огня → «COMBO! Огненная буря» →
   увеличенный AoE + урон
3. Комната ⏳: счётчик «6» виден, каждый ход −1,
   при 0 → все получают 99 урона
4. Комната 🌑: видны только 2 клетки вокруг игрока,
   остальное чёрное (но враги по краю видимости — видны)
5. Комната 🔄: после каждого хода все сдвигаются вправо (стрелки),
   враг у стены — не двигается
6. На карте: модификатор виден как иконка рядом с узлом ⚔️
```

---

## B2 — Мета-прогрессия: рецепты и реликвии

**Цель**: рецепты и реликвии разблокируются через ачивки.
Начальный пул ограничен.

### Фичи

1. **AchievementService и AchievementTracker**
   - `AchievementTracker`: подписка на игровые события,
     обновление прогресса в `MetaProgressionModel`
   - Условия: `AccumulateStat`, `SingleRunCondition`, `CombatCondition`
   - При выполнении → разблокировка + нотификация + сохранение

2. **Разблокировка рецептов (7 ачивок)**
   - Стартовые: Пламя, Поток, Отрава, Пар, Напалм, Кислота
   - Молния: «Убей 3 врагов зельем воды за 1 ран»
   - Вспышка: «Дойди до Этажа 2»
   - Проклятие: «Убей элитного без урона»
   - Бездна: «Толкни 5 врагов»
   - Спора: «Используй фонтан 3 раза»
   - Миазма: «Нанеси ядом 30 урона»
   - Хаос: «Открой все рецепты»
   - Неразблокированные рецепты: merge даёт «неизвестное зелье»
     (визуально — серая колба с «?»), не попадает в инвентарь

3. **Разблокировка реликвий (5 оставшихся)**
   - Мутаген, Декантер, Шип, Вихрь, Монокль
   - Каждая привязана к ачивке (по GDD)
   - Неразблокированные не появляются в пуле выбора после элиты

### Приёмочный тест (Play Mode)

```
1. Новый профиль (сброс прогресса)
2. Доступно: Пламя, Поток, Отрава, Пар, Напалм, Кислота
3. Merge 🔵+🟡 → серая колба «?» (Молния не разблокирована)
4. Убить 3 врагов Потоком за ран → нотификация
   «Рецепт разблокирован: Молния!»
5. В следующем ране 🔵+🟡 → Молния lv1 (работает)
6. Книга рецептов: разблокированные — с описанием,
   остальные — «?» с условием разблокировки
7. Победить элиту → выбор реликвий только из разблокированных
8. Разблокировать Декантер → зоны длятся +1 ход (проверить)
```

---

## B3 — Мета-прогрессия: улучшения

**Цель**: постоянные улучшения разблокируются и применяются.
Экран ачивок функционален.

### Фичи

1. **Постоянные улучшения (5 штук)**
   - 5-й слот зелий: «Победи финального босса»
     → InventoryModel.maxSlots = 5
   - Расширенная доска 5×4: «Используй все 5 элементов за ран»
     → BoardModel.width = 5
   - 4-е действие: «Создай по 1 зелью каждого рецепта»
     → maxActions = 4
   - Стартовое зелье: «Дойди до этажа 3 три раза»
     → при старте рана — случайное зелье lv1 в инвентаре
   - Дальнозоркость: «Пройди 10 событий»
     → на карте видно на 2 ряда вперёд

2. **Экран ачивок**
   - `AchievementScreen`: список ачивок по категориям
     (Рецепты, Реликвии, Улучшения)
   - Каждая ачивка: название, описание, прогресс-бар,
     награда, статус (✓ / в процессе)
   - Доступ из меню (кнопка 🏆)

3. **Применение улучшений при старте рана**
   - `RunFactory.CreateRun(MetaProgressionModel)`:
     проверить unlocked upgrades, применить
   - 5-й слот: UI адаптируется (5 слотов вместо 4)
   - 5×4 доска: BoardView адаптируется, генерация с width=5
   - 4 действия: 4 точки вместо 3

### Приёмочный тест (Play Mode)

```
1. Открыть экран ачивок → 3 категории,
   прогресс отображается
2. Победить финального босса →
   «Улучшение: 5-й слот для зелий!»
3. Следующий ран → 5 слотов внизу экрана
4. Разблокировать расширенную доску → перегонка 5×4 (20 ячеек)
5. Разблокировать 4-е действие → 4 точки действий
6. Разблокировать стартовое зелье →
   ран начинается с 1 зельем в инвентаре
7. Разблокировать дальнозоркость →
   на карте видны узлы на 2 ряда вперёд
8. Все улучшения сохраняются между сессиями
```

---

## B4 — Обучение

**Цель**: первый ран — обучающий. 5 шагов без текстовых стен.

### Фичи

1. **TutorialController и TutorialOverlay**
   - `TutorialController`: FSM обучения, 5 шагов
   - `TutorialOverlay`: затемнение экрана кроме целевой области,
     анимированная стрелка, пульсирующая подсветка
   - `InputFilter`: блокировка ввода вне целевой области

2. **5 шагов обучения**
   - Шаг 1 (Merge): доска только 🔴 и 🔵, стрелка 🔴→🔴.
     Игрок мержит. Затем 🔵→🔵. Зелья в слотах
   - Шаг 2 (Первый бой): 4×4 сетка, 1 скелет.
     Подсветка: «тапни зелье → тапни клетку». Убить
   - Шаг 3 (Кулдаун и движение): 2 скелета. Бросок → кулдаун,
     подсказка второго зелья. Свайп для движения
   - Шаг 4 (Рецепт и подпитка): 🔴, 🔵, 🟢 на доске.
     Стрелка 🔴→🟢 (рецепт Напалм). Стрелка 🔴→варево (подпитка).
     Книга рецептов мигает
   - Шаг 5: свободная игра, карта этажа без подсказок

3. **Условия пропуска**
   - Если `MetaProgressionModel.tutorialCompleted == true` →
     обычный ран
   - Кнопка «Пропустить обучение» (мелкий текст в углу)
   - По завершению: tutorialCompleted = true, сохранить

### Приёмочный тест (Play Mode)

```
1. Сброс прогресса → новый профиль
2. «Старт» → обучающая доска (только 🔴 и 🔵)
3. Стрелка от 🔴 к 🔴 → перетащить → merge →
   «Пламя lv1» в слоте (подсветка)
4. Стрелка от 🔵 к 🔵 → merge → «Поток lv1»
5. Автопереход в бой → 1 скелет, 4×4 сетка
6. Подсветка: «тапни зелье» → тап → «тапни клетку» →
   тап → скелет умирает
7. Перегонка → бой 2: 2 скелета, кулдаун после первого броска,
   подсказка движения (свайп)
8. Перегонка → 🔴, 🔵, 🟢: стрелка 🔴→🟢 → Напалм,
   стрелка 🔴→варево → подпитка, 📖 мигает → тап → книга
9. Свободная игра далее — без подсказок
10. Второй ран: обучение не повторяется
11. Кнопка «Пропустить» → обучение заканчивается сразу
```

---

## B5 — Juice (часть 1): анимации

**Цель**: все действия имеют приятную визуальную отдачу.
Merge, бросок, попадание, смерть — всё анимировано.

### Фичи

1. **Tween-система**
   - `TweenRunner`: MonoBehaviour, список активных tweens,
     обновление в Update
   - Типы: `TweenPosition`, `TweenScale`, `TweenAlpha`,
     `TweenFloat`, `TweenSequence`, `TweenParallel`
   - Ease-функции: Linear, EaseInOut, EaseOutElastic,
     EaseOutBounce, EaseOutBack
   - API: `Tween.Position(transform, target, duration, ease)`

2. **Анимации перегонки**
   - Merge: ингредиенты притягиваются (lerp 0.2s) →
     вспышка → shockwave (ring SDF) →
     варево bounce-in (EaseOutElastic 0.3s)
   - Подпитка: ингредиент летит к вареву (0.15s) →
     glow flash → жидкость поднимается (0.2s)
   - Сбор в инвентарь: варева летят к слотам
     (bezier curve, stagger 0.1s) → bounce при приземлении
   - Растворение оставшихся ингредиентов: fade out с частицами

3. **Анимации боя**
   - Бросок зелья: слот пульсирует → «снаряд» летит
     по параболе (0.25s) → взрыв / эффект
   - Попадание: враг мигает белым (0.05s) → shake (3px, 0.1s)
   - Число урона: появляется над врагом, bounce вверх (0.6s),
     fade out
   - Движение игрока: squash & stretch (сжатие в направлении
     движения)
   - Движение врага: аналогичный squash & stretch
   - Толчок: враг «отлетает» (быстрый lerp + bounce at wall)

### Приёмочный тест (Play Mode)

```
1. Merge: два ингредиента плавно слетаются, вспышка,
   кольцо-шоквейв, варево «пружинит» при появлении
2. Подпитка: ингредиент влетает в варево, жидкость
   плавно поднимается
3. Сбор: варева по очереди взлетают в слоты зелий
4. Бросок: «снаряд» летит дугой от персонажа к клетке
5. Попадание: белая вспышка на враге, число урона
   «подпрыгивает» над ним
6. Движение: персонаж слегка сплющивается в направлении хода
7. Толчок: враг быстро отлетает, при ударе о стену — squash
8. Все анимации плавные (60 FPS), нет frame drops
```

---

## B6 — Juice (часть 2): эффекты и пост-процесс

**Цель**: combo-эффекты, dissolve, slowmo, полный пост-процессинг.

### Фичи

1. **Combo juice**
   - При combo: `ShaderGlobalController.SetSlowmo(0.05, 0.15s)` →
     мир замедляется, эффект усиленный
   - Текст «COMBO» (scale up + glow, золотой) → fade out
   - Расширенные VFX: ×2 количество частиц, ×1.5 размер
   - Screenshake усиленный (8px, 0.25s)
   - Fullscreen flash (белый, 0.15s)

2. **Dissolve при смерти**
   - `SH_Dissolve` (функция в EntityCommon.cginc):
     noise threshold → discard fragments + emission edge
   - TweenFloat: `_DissolveProgress` 0→1 за 0.4s
   - Частицы разлетаются от позиции врага (8–12 частиц, radial)
   - «Звёздочки опыта» летят к игроку (bezier curve)

3. **Полный пост-процессинг**
   - `SH_PP_Final`: виньетка (всегда) + хроматическая аберрация
     (при ударе, 0.1s) + damage vignette (красная, при уроне)
   - `SH_FX_DamageNumber`: 7-segment SDF, bounce ease,
     тёмный контур для читаемости
   - Hitstop: `Time.timeScale = 0.05` на 0.05s при каждом
     попадании зельем

### Приёмочный тест (Play Mode)

```
1. Combo: Молния в воду → мир замедляется на миг →
   усиленная молния → «COMBO!» золотым → screenshake
2. Убить врага → dissolve (фрагменты исчезают с яркой каймой) →
   частицы разлетаются → звёздочки летят к игроку
3. Получить урон → красная вспышка по краям экрана,
   хроматическая аберрация на 0.1s
4. Попадание зельем → мир на миг застывает (hitstop),
   затем числа урона с bounce
5. Виньетка видна всегда (края темнее центра)
6. Все эффекты не роняют FPS ниже 55
```

---

## B7 — Баланс, качество, книга

**Цель**: игра сбалансирована, есть fallback для слабых устройств,
книга алхимика полная.

### Фичи

1. **Баланс**
   - Проверить таблицу урона (GDD секция 18):
     все значения в DamageService соответствуют таблице
   - Прогон BalanceSimulator: 1000 ранов, целевой win rate 35–45%
   - Если win rate вне диапазона: скорректировать HP врагов,
     урон зелий, количество врагов
   - Ручные плейтесты: 5+ человек, 3 сессии каждый
   - Фиксация на основе обратной связи

2. **Система качества и fallback**
   - `QualityController`: автоопределение
     (GPU level, device type, runtime FPS)
   - `QUALITY_HIGH` / `QUALITY_LOW` keywords
   - LOW: noise из LUT, fbm 2 октавы, без каустики,
     без хром. аберрации, bloom в 2 pass
   - Настройка screenshake в Settings (слайдер 0–100%)

3. **Книга алхимика и настройки**
   - `BookScreen`: полная — все рецепты, карточки с описанием,
     AoE-визуализация, урон по уровням
   - Доступ: из меню + из боя (кнопка 📖)
   - Неразблокированные: «?» с подсказкой условия
   - Экран настроек: SFX, музыка, вибрация, screenshake,
     подтверждение хода, сброс прогресса
   - Сохранение настроек в PlayerPrefs

### Приёмочный тест (Play Mode)

```
1. Симулятор: 1000 ранов, win rate в логе.
   Если 35–45% — ОК
2. На слабом устройстве (или эмуляция LOW): всё читаемо,
   ≥50 FPS, bloom есть но попроще
3. Книга: все 15 зелий, тап → карточка с описанием,
   AoE-паттерн, урон по уровням
4. Неразблокированные рецепты показывают «?»
   и условие разблокировки
5. Настройки: screenshake = 0% → нет тряски,
   вибрация выкл → нет вибрации
6. Сброс прогресса → подтверждение → всё обнулено
7. 5 плейтестеров: средний ран 8–12 минут,
   никто не stuck, fun factor ≥ 7/10
```

---

## Чеклист завершения Beta

- [ ] Все 7 combo работают с визуальными эффектами
- [ ] Все 6 модификаторов комнат работают
- [ ] Мета-прогрессия: все ачивки отслеживаются и разблокируются
- [ ] Все рецепты разблокируются через ачивки
- [ ] Все реликвии разблокируются через ачивки
- [ ] Все 5 постоянных улучшений работают
- [ ] Обучение: 5 шагов, ~2 минуты, можно пропустить
- [ ] Juice: merge анимации, бросок с параболой, dissolve,
      hitstop, combo slowmo, screenshake, числа урона
- [ ] Полный пост-процессинг: bloom, виньетка,
      хроматическая аберрация, flash
- [ ] Баланс: win rate 35–45%, средний ран 8–12 мин
- [ ] Quality fallback: LOW на слабых GPU, ≥50 FPS
- [ ] Книга алхимика полная
- [ ] Настройки: звук, вибрация, screenshake, сброс
- [ ] Save/Load стабилен, миграция версий
```

---

## Файл 4: `iterations_release.md`

```markdown
# Release — Итерации

> **Цель фазы**: готовность к публикации в Google Play.
> Звук, финальный полиш, оптимизация, сборка.
>
> **Предусловие**: Beta завершена — полная игра,
> сбалансированная, с juice.
>
> **Длительность**: 3–4 недели (5 итераций × 3–4 дня)

---

## R1 — Звук

**Цель**: все игровые действия озвучены. Музыка играет.

### Фичи

1. **SFX для всех событий**
   - Генерация через sfxr / jsfxr (синтетические звуки)
   - Категории:
     - Merge: «буль» + rising tone
     - Подпитка: шипение + chime
     - Бросок зелья: whoosh (per element color)
     - Попадание: short noise burst (per damage type)
     - Убийство: sharp decay + sparkle
     - Combo: layered impact + reverb
     - Движение: soft step
     - Толчок: thud
     - Получение урона: crunch + low rumble
     - UI: click, whoosh, page flip
     - Событие: chime (фонтан), coins (торговец),
       dark tone (алтарь), chest open (сундук)
   - `AudioService`: подписка на события → воспроизведение SFX
   - Пул из 8 AudioSource для SFX

2. **Музыка**
   - 4 ambient loop-а (60–90 сек каждый):
     - Этаж 1: спокойный, загадочный (low pad + drip)
     - Этаж 2: напряжённый (rhythmic pulse + bass)
     - Этаж 3: тревожный (dissonant pad + heartbeat)
     - Босс: интенсивный (percussion + tension)
   - Crossfade между треками (1 сек) при смене этажа / входе в босса
   - 2 AudioSource для crossfade
   - Музыка приглушается при открытии popup-ов (duck to 30%)

3. **Haptic (вибрация)**
   - Лёгкая вибрация: попадание, merge
   - Средняя: combo, убийство
   - Сильная: получение урона, смерть
   - Настройка: вкл/выкл в Settings
   - `Handheld.Vibrate()` с разной длительностью
     (или Unity Input System Haptics при наличии)

### Приёмочный тест (Play Mode)

```
1. Merge: слышен «буль» при слиянии
2. Бросок Пламени: whoosh → impact
3. Убийство: sparkle звук + dissolve
4. Combo: усиленный impact + reverb
5. Получение урона: crunch + вибрация (если включена)
6. Музыка этажа 1: спокойный фон
7. Переход на этаж 2: плавный crossfade к напряжённому треку
8. Вход в босс-бой: интенсивный трек
9. Громкость SFX и музыки управляется из настроек
10. Мьют при сворачивании приложения
    (AudioListener.pause = true при OnApplicationPause)
```

---

## R2 — Переходы и UI-полиш

**Цель**: плавные переходы между экранами.
Нотификации разблокировок. Финальный UI-полиш.

### Фичи

1. **Анимации переходов**
   - `TransitionOverlay`: fullscreen quad с alpha-анимацией
   - Типы:
     - FadeToBlack: alpha 0→1 (0.2s) → callback → 1→0 (0.2s)
     - SlideLeft: текущий экран уезжает влево, новый —
       въезжает справа (0.3s)
     - CrossDissolve: noise-dissolve текущего экрана,
       одновременно появление нового (0.4s)
   - Использование:
     - Menu → Distillation: FadeToBlack
     - Distillation → Combat: SlideLeft
     - Combat → Map: FadeToBlack
     - Map → Event: FadeToBlack
     - Floor → Floor: CrossDissolve (dramatic)

2. **Нотификации разблокировок**
   - `NotificationOverlay`: плашка сверху экрана,
     slide-in → hold 2s → slide-out
   - Типы: «Рецепт разблокирован!» (с иконкой зелья),
     «Реликвия разблокирована!», «Улучшение получено!»,
     «Ачивка выполнена!»
   - Очередь: если несколько нотификаций — показывать
     последовательно (stagger 0.5s)

3. **UI-полиш**
   - Все кнопки: SH_UI_Glow (пульсирующее свечение
     для главных кнопок, статичное для вторичных)
   - Кнопка «Старт»: усиленный glow-pulse
   - Кнопки в popup-ах: hover-feedback (scale ×1.05 при нажатии)
   - Лаборатория (фон меню): анимированный шейдер
     (котёл с дымом + пузырьки + мерцающие ингредиенты)
   - Result screen: анимированный подсчёт статистики
     (числа «набегают» с counting animation)
   - HP hearts: SH_UI_HealthHeart (SDF-сердечко, пульс при потере)

### Приёмочный тест (Play Mode)

```
1. Меню → «Старт»: плавный fade to black → доска появляется
2. Перегонка → бой: экран «уезжает» влево,
   бой «въезжает» справа
3. Бой → карта: fade
4. Этаж 1 → этаж 2: noise-dissolve (красиво)
5. Разблокировка рецепта → плашка сверху «Рецепт: Молния!»,
   slide-in, задержка, slide-out
6. Две разблокировки одновременно → показываются по очереди
7. Кнопка «Старт» пульсирует свечением
8. Result screen: числа «набегают» (0 → 7 врагов убито, 1 sec)
9. Фон меню: анимированный котёл с дымом
10. HP hearts — SDF-сердечки, пульсируют при потере HP
```

---

## R3 — Оптимизация

**Цель**: стабильные 60 FPS на минимальном устройстве.
Размер APK < 50 MB.

### Фичи

1. **Профилирование и фиксация**
   - Тест на минимальном устройстве (или эмуляция:
     Android 8.0, 2 GB RAM, Mali-T720)
   - Unity Profiler: найти CPU bottleneck-и
     (GC alloc, expensive Update)
   - Frame Debugger: проверить draw call count
     (цель: ≤20 per frame)
   - Исправления:
     - Struct events вместо class (если ещё не сделано)
     - Shader.PropertyToID кэширование
     - List<> pre-allocation
     - Удаление LINQ из hot paths
     - Object pool verification (нет runtime Instantiate
       во время геймплея)

2. **Шейдерные fallback-и**
   - Проверить все шейдеры в режиме `QUALITY_LOW`
   - Noise LUT: проверить что выборка текстуры используется
   - Bloom: 2 pass вместо 4 на LOW
   - Без хроматической аберрации на LOW
   - Без каустики в воде на LOW
   - Визуальная проверка: всё читаемо и приемлемо на LOW

3. **Размер билда**
   - Managed Stripping Level: Medium → High
     (проверить что ничего не порезано лишнего)
   - Проверить размер текстур (LUT 256×256, Ramp 256×4,
     UI atlas 512×512 — должно быть < 1 MB total)
   - Аудио: сжатие (Vorbis, quality 50% для SFX,
     70% для музыки)
   - Нет лишних ассетов в билде (Editor-only файлы исключены)
   - Целевой APK: < 50 MB (AAB может быть меньше)

### Приёмочный тест (Play Mode)

```
1. На минимальном устройстве:
    - Загрузка < 3 сек
    - FPS ≥ 55 стабильно (все сцены)
    - Пиковый момент (combo + 3 врага + зоны): FPS ≥ 45
    - RAM < 200 MB
2. Unity Profiler: 0 B GC alloc per frame во время геймплея
3. Frame Debugger: ≤ 20 draw calls per frame
4. Quality LOW: визуально приемлемо, все объекты читаемы,
   bloom есть, анимации плавные
5. APK size: < 50 MB
6. Батарея: < 10% за 30 минут игры
7. Нет thermal throttling за 15 минут непрерывной игры
```

---

## R4 — Сборка и публикация

**Цель**: APK/AAB собран, подписан, загружен в Google Play Console.

### Фичи

1. **Настройка билда**
   - IL2CPP backend, ARM64
   - Keystore: создать и сохранить (secure backup!)
   - Подпись AAB
   - `ApplicationIdentifier`: `com.studio.alchemistsdescent`
   - Version: 1.0.0, versionCode: 1
   - Min API: 26 (Android 8.0), Target API: 34 (Android 14)
   - Иконка: адаптивная (foreground SDF-рендер → PNG export,
     background solid dark)
   - Splash screen: минимальный (логотип 1 сек)

2. **Google Play Console**
   - Создать приложение
   - Описание: короткое (80 символов) и полное (4000 символов)
   - Скриншоты: 5+ (из игры, 1080×1920)
   - Feature graphic: 1024×500
   - Категория: Casual / Roguelike
   - Content rating: заполнить опросник (IARC)
   - Privacy policy: простая страница (нет сбора данных)
   - Internal testing track: загрузить AAB

3. **Crash reporting (опционально)**
   - Firebase Crashlytics: интеграция
     (или Unity Cloud Diagnostics — бесплатно)
   - Проверить: crash при startup, crash при save/load,
     ANR detection

### Приёмочный тест (Play Mode)

```
1. AAB собирается без ошибок
2. Установка на устройство из AAB (bundletool): работает
3. Splash → меню за < 3 сек
4. Полный ран проходится без crash-ей
5. Сворачивание / разворачивание: состояние сохранено
6. Уведомления не мешают (overlay notification → игра на паузе)
7. Google Play Console: AAB загружен,
   проходит pre-launch тесты
8. Internal testing: ссылка работает,
   3 тестера установили и прошли ран
```

---

## R5 — Финальное QA и релиз

**Цель**: все баги исправлены. Приложение опубликовано.

### Фичи

1. **Полный QA-прогон**
   - Чеклист из архитектурной документации (секция 21.4)
   - Все экраны, все переходы
   - Все зелья, все враги, все боссы
   - Все события, все реликвии
   - Все ачивки (проверить что срабатывают)
   - Все модификаторы комнат
   - Все combo
   - Save/load: mid-run + мета
   - Обучение: полный прогон + пропуск
   - 3 устройства: low-end, mid-range, flagship

2. **Bug fixing**
   - Критические: crashes, data loss, infinite loops
   - Major: gameplay-breaking (невозможно пройти,
     невалидные состояния)
   - Minor: визуальные глитчи, неточные описания
   - Won't fix: косметические, не влияющие на геймплей
   - Приоритет: Critical → Major → Minor

3. **Публикация**
   - Closed testing (если нужно): 20–50 тестеров,
     1 неделя, сбор фидбэка
   - Production release: поэтапный rollout (10% → 50% → 100%)
   - Мониторинг: crash rate < 1%, ANR rate < 0.5%,
     rating ≥ 4.0
   - Hotfix-процесс: fast-track для critical bugs
     (fix → build → upload → review → release, < 48 часов)

### Приёмочный тест (Play Mode)

```
1. Полный прогон чеклиста (все пункты ✓)
2. 0 Critical bugs, 0 Major bugs
3. 10 полных ранов на 3 устройствах без crash-ей
4. Crash rate в Firebase / Cloud Diagnostics: 0% за тест-период
5. 3 внешних тестера (closed testing):
    - Все прошли обучение
    - Все прошли минимум 5 ранов
    - Средняя оценка ≥ 4/5
6. Production release: rollout 10%,
   мониторинг 24 часа, нет всплеска crash-ов
7. Rollout 100%
```

---

## Чеклист релиза

- [ ] Все SFX озвучены (merge, бросок, попадание, смерть,
      combo, UI, события)
- [ ] Музыка: 4 трека, crossfade, duck
- [ ] Вибрация: лёгкая / средняя / сильная, настройка в Settings
- [ ] Переходы: fade, slide, dissolve — все плавные
- [ ] Нотификации разблокировок работают
- [ ] UI-полиш: glow кнопки, анимированный фон меню,
      counting на результате
- [ ] 60 FPS на mid-range, 50+ на low-end
- [ ] 0 B GC alloc per frame
- [ ] ≤ 20 draw calls
- [ ] APK < 50 MB
- [ ] Quality fallback: LOW визуально приемлем
- [ ] AAB подписан, загружен в Google Play Console
- [ ] Описание, скриншоты, feature graphic, категория,
      content rating, privacy policy
- [ ] Crashlytics / Cloud Diagnostics подключены
- [ ] 0 Critical, 0 Major bugs
- [ ] Closed testing пройден (3+ тестера, ≥ 4/5)
- [ ] Production rollout: 10% → 50% → 100%
- [ ] Мониторинг: crash rate < 1%, ANR < 0.5%

---

## Суммарный таймлайн всех итераций

| Фаза | Итераций | Длительность | Накопительно |
|------|----------|-------------|--------------|
| **MVP** (M1–M6) | 6 | 4–5 недель | 1 месяц |
| **Alpha** (A1–A8) | 8 | 5–6 недель | 2.5 месяца |
| **Beta** (B1–B7) | 7 | 4–5 недель | 3.5 месяца |
| **Release** (R1–R5) | 5 | 3–4 недели | 4.5 месяца |
| **Итого** | **26 итераций** | **16–20 недель** | **~4.5 месяца** |
```